function myfit(x,y)
y1=fliplr(y)/fliplr(max(y));
x1=fliplr(x)/fliplr(max(x));
%???????????????????

ymax=max(y); %??y ??????????????



%% Calculate coefficient  ???????????????[01] ?????fittype??????
fun=fittype('A*exp(-(x-mu)^2/(2*sigma^2))');
gaufitoption = fitoptions('gauss2');
[cf,gof]=fit(x1(:),y1(:),fun,gaufitoption);
%??????????cf ?
%% Interpolate the data
%????????????????????
k=1:length(x1);
ki=linspace(1,length(x1),20);
xi=interp1(k,x1,ki,'linear');
yi=interp1(k,y1,ki,'linear');
%?????????Y????y??????
Yi=cf.A*exp(-(xi-cf.mu).^2/(2*cf.sigma^2))*ymax;



%% plot data
plot(x,y,'ro');  % ????
hold on
plot(xi*max(x),Yi,'b');  %????
end